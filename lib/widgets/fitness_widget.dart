import 'package:flutter/material.dart';
import 'package:flutter_app_sashko/models/list_item.dart';

class FitnessWidget extends StatelessWidget {

  ListItem item;

  FitnessWidget({required this.item});

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}
